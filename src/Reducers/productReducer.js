
const initialState ={
productList:[],
filteredLit:[],
fetching:false,
fetched:false
}

export default function reducer (state = initialState, action){

switch (action.type) {
  case "CASE_NAME":{

    break;
  }

    break;
  default: return state;

}

    return state;
}
